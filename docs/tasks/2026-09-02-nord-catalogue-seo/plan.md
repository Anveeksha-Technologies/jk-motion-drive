# Implementation Plan — NORD Catalogue + SEO/GEO/GA

Spec: `docs/superpowers/specs/2026-09-02-nord-catalogue-seo-design.md`
Branch: `feat/nord-catalogue-seo`

## Phase A — Data layer

- [x] A1. `content/glossary.json` — new file. `{ "terms": [{ "term", "short", "long" }] }`.
      Terms: Sizes, Power, Torque, Ratio, Voltage, UNICASE, NORDBLOC.1,
      MAXXDRIVE, nsd tupH, POSICON, ISD control, Integrated PLC, IE1–IE4.
      Definitions drawn from flyer F1300 pages 4–10.
- [x] A2. `content/products.json` — add `"nsdTupH": true|false` to all 24 products.
      `true` for: NORDBLOC.1 2-Stage Bevel Gear Units, UNIVERSAL SMI Worm Gear
      Units, Smooth Motors, NORDAC BASE SK 180E, NORDAC START SK 135E.
      All others `false`. (See spec: the sixth page-10 entry has no clean row.)
- [x] A3. `lib/products.ts` — extend `RawProduct` with `nsdTupH: boolean`, add
      `nsdTupH` to `SubProduct` type and to the `subProducts` map.
- [x] A4. `lib/glossary.ts` — typed boundary over `glossary.json`, exporting
      `glossary: Record<string, GlossaryTerm>` keyed by lowercased term.
- [x] A5. `scripts/check-content.mjs` — validate `nsdTupH` is boolean on every
      product; validate `glossary.json` shape.

## Phase B — Catalogue page

- [x] B1. `lib/catalogue.ts` — `CatalogueRow` type; `catalogueRows` flattening
      `rawCategories` into 24 rows (category slug/title attached, specs mapped
      to a `Record<string,string>` by label, image resolved); `SPEC_COLUMNS`
      and `columnsFor(categorySlug)` implementing the adaptive column rule.
- [x] B2. `components/SpecTooltip.tsx` — `"use client"`. Focusable `<button>`
      wrapping a lucide `Info` icon, `aria-describedby`, Escape-to-dismiss,
      click-outside close. Renders nothing when the term is not in the glossary.
- [x] B3. `components/CatalogueTable.tsx` — `"use client"`. Search input
      (title/catalogue/tags/specs/category), category filter pills, adaptive
      columns, 10-rows-per-page pagination, image column via `next/image`,
      result count, empty state. Uses existing semantic classes.
- [x] B4. `app/catalogue/page.tsx` — server component. `PageHero` +
      intro + `<CatalogueTable />` + `<CtaBanner />`. Exports `metadata`.
- [x] B5. `lib/site.ts` — add `{ href: "/catalogue", label: "Catalogue" }` to
      `primaryNav` after Products.

## Phase C — Generated Markdown

- [x] C1. `scripts/gen-catalogue.mjs` — reads `content/products.json` +
      `glossary.json`, writes `docs/nord-product-catalogue.md`.
      Provenance header (F1300, Mat.-Nr. 6021602 / 1118), one section and table
      per category using only that category's real columns, nsd tupH list,
      glossary appendix. `--check` mode exits non-zero on drift.
- [x] C2. `package.json` — add `"gen:catalogue"` script; chain `--check` into
      `check:content`.
- [x] C3. Run generator, stage `docs/nord-product-catalogue.md`.

## Phase D — SEO / GEO / LLM / Analytics

- [x] D1. `lib/seo.ts` — `siteUrl` from `NEXT_PUBLIC_SITE_URL`,
      `buildMetadata({title, description, path})`, `organizationLd()`,
      `localBusinessLd()`, `breadcrumbLd()`.
- [x] D2. `app/layout.tsx` — `metadataBase`, OpenGraph, Twitter, robots, icons,
      `alternates.canonical`; `geo.*` meta; JSON-LD script; `<Analytics />`.
- [x] D3. Per-route `metadata` for `/`, `/about`, `/products`, `/industries`,
      `/gallery`, `/contact`, `/catalogue`.
- [x] D4. `app/sitemap.ts` + `app/robots.ts`.
- [x] D5. `public/llms.txt` — generated by `gen-catalogue.mjs` alongside the MD.
- [x] D6. `components/Analytics.tsx` + `.env.example`.
- [x] D7. `app/icon.svg` — brand-coloured mark (favicon).
- [ ] D7b. **Not done:** dedicated 1200x630 `opengraph-image`. OG currently
      points at `/images/logo.webp`, which is the brand lockup and not that
      aspect ratio, so social previews will letterbox. Needs real artwork from
      the client — listed in the validation report as outstanding.

## Phase E — Docs + validation

- [x] E1. `CLAUDE.md` — amend the client-component line; document `/catalogue`,
      the generated MD and the SEO surface.
- [x] E2. `content/README.md` — document `nsdTupH`, `glossary.json`, generator.
- [x] E3. `npm run check:content`, `next build`, route smoke test on :3210.
- [x] E4. `validation-report.md` in this directory.
- [x] E5. Stage all changes; summarise; **do not commit** (user commits).
